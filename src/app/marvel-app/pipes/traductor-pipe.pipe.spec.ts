import { TraductorPipePipe } from './traductor-pipe.pipe';

describe('TraductorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TraductorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
